- git add .
- git commit -m "Cập nhật file mới"
- git push

- do lỗi khi nhập nhầm lẫn nhập lệnh: (rm -rf .git && git init && git add . && git commit -m "Khởi tạo lại repo từ đầu" && git branch -M main && git remote add origin https://github.com/<ten_cua_ban>/<ten_repo>.git && git push -f origin main)
- gây ra mất toàn bộ commit cũ.
  
